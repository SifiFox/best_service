import LoginPreview from '@/widgets/auth/login/login-preview';

export default function LoginPage() {
  return (
    <div className="flex w-fit items-center h-fit justify-center self-center py-20">
      <LoginPreview />
    </div>
  );
}
