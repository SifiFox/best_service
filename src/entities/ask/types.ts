import { AskCreate, AskResponse } from '@/shared/types/api';

// Типы для вопросов
export type Ask = AskResponse;

export type AskCreateRequest = AskCreate;

export type AskCreateResponse = AskResponse;
